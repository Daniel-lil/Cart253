//class which contains the function that displays the sphere pattern background
class Instructions {
constructor(){
}

//displays box for Instructions
display(){
  push();
  fill(0);
  stroke(255);
  rect(-width/2,0 + height/2.3,width, 1000);
  pop();
  //displays writing for instructions
  push();

  pop();

}
